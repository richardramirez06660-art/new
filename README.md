# ISHMP4

Modern görünümlü Flask tabanlı video kaydetme sitesi.

## Kurulum

```bash
pip install -r requirements.txt
python app.py
```

## Render
- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn app:app`
